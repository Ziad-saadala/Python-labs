import math #should import library to use it below

r = float(input('enter radius:'))
if r<=0: 
    print('invalid') #print string shoud be in quotations and indetation was wrong
else:
    area = math.pi * r * r      #wrong indetation and didnt use keyword to use library command


print(area) # vriable shouldn be in quotation